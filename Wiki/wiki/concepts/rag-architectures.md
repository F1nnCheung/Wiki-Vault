---
title: RAG 架构：从经典检索到智能体驱动
type: concept
tags: [rag, knowledge-base, retrieval, graph-rag, agentic-rag, ai, evolution]
created: 2026-05-28
updated: 2026-06-03
sources:
  - raw/articles/知识库/一文看懂三种 RAG 架构：Classic RAG、Graph RAG 与 Agentic RAG.md
  - raw/articles/知识库/AI 知识库技术演进拆解：从 RAG 到 NotebookLM，再到 LLM Wiki.md
  - raw/articles/知识库/RAG我懂你：从架构到知识库构建.md
  - raw/articles/知识库/RAG 进化史：从基础检索到智能体驱动.md
  - raw/articles/知识库/RAG：发展演进全景.md
  - raw/articles/知识库/第 8 期：企业为什么需要 RAG，而不是直接微调模型.md
  - raw/articles/知识库/RAG 多模态 与 LLM+工具链.md
  - raw/articles/知识库/RAG、Agentic RAG 和 AI Memory 到底有什么区别？.md
  - raw/articles/知识库/前沿进展：LLM-Wiki自进化RAG及MinerU-Popo文档解析后处理设计思路.md
  - raw/articles/知识库/做了 5 个知识库项目后，我整理了一份 RAG 选型终极指南.md
related:
  - concepts/agentic-engineering.md
  - concepts/second-brain.md
  - topics/obsidian-llm-wiki-practice.md
  - concepts/code-knowledge-graph.md
  - topics/rag-optimization-techniques.md
  - concepts/rag-evaluation.md
  - concepts/hybrid-retrieval.md
  - topics/agentic-rag-patterns.md
  - topics/local-rag-setup-guide.md
  - topics/production-rag-architecture.md
---

# RAG 架构：从经典检索到智能体驱动

RAG（Retrieval-Augmented Generation，检索增强生成）的核心思路：**先把相关资料找出来，再让大模型基于这些资料回答**——让模型从「凭印象回答」变为「带着资料回答」。自 2020 年 Meta 提出这一概念以来，RAG 已从学术论文逐步成为企业级 AI 应用的主流方案之一，经历了五个阶段的演进。

三个动词概括三种架构的本质差异：
- **Classic RAG**：检索（Retrieves）——找资料
- **Graph RAG**：连接（Connects）——找关系
- **Agentic RAG**：推理（Reasons）——决定下一步该查什么

---

## RAG 五阶段演进史

在深入三种架构之前，先了解 RAG 是如何从最简单的框架逐步演进到智能体驱动的。这个演进不是每一步都"替代"前一步——它们是**逐层叠加**的关系。

### 第一阶段：Naive RAG（基础框架，2020-2022）

从文档到回答的三个步骤，类比图书馆运作：先把书拆成章节编目上架，读者提问时检索书架，找到相关内容后用通俗语言总结回答。

1. **索引（Indexing）**：提取纯文本 → 分割成 Chunk → Embedding 编码为向量 → 存入向量数据库
2. **检索（Retrieval）**：用户查询 → Embedding → 向量相似度计算 → 返回 Top-K 文本块
3. **生成（Generation）**：原始查询 + 检索文档块 → 拼接为 Prompt → LLM 生成答案

Naive RAG 的问题很明显——单次检索，没有回头路；检索噪声直接影响生成质量；分块策略粗放。

### 第二阶段：Advanced RAG（链路升级，2022-2023）

针对 Naive RAG 检索环节的结构性瓶颈进行四个方向的优化：

- **查询重写**：HyDE（先假设答案再检索）、Multi-Query（多版本并行检索）、Step-back Prompting（抽象到上位概念）
- **语义分块**：用 Embedding 模型计算相邻段落相似度，语义差异超阈值时才分块；Agentic Chunking 更进一步——用 LLM 进行自适应边界判定
- **混合搜索**：BM25（词法精确匹配）+ 向量检索（语义理解）并行，RRF 融合
- **重排序**：Reranker 对初检结果做二次排序（Cohere Rerank、BGE-Reranker、ColBERT）

但这些优化本质上都是在**预定义的流水线**内增加环节——数据仍然从前一个节点单向流向后一个节点，没有分叉也没有回路。

### 第三阶段：Modular RAG（组件化架构，2023-2024）

将检索和生成的流水线分解为独立且可重用的组件。系统不再是"文档进来、答案出去"的固定流水线，而是由可插拔模块组成的可组合流水线。

- 模块间交互不再局限于单向顺序执行，支持条件分支、并行检索和循环迭代
- 典型方法：FLARE（根据置信度动态触发检索）、CRAG（检索质量不足时降级到网络搜索）、ITER-RETGEN（生成输出反馈为下一轮检索输入）
- 主流框架（LlamaIndex、LangChain、Haystack）原生支持模块化编排

Modular RAG 的核心价值：将系统搭建从"逐段硬编码"转向"声明式组装"。

### 第四阶段：Agentic RAG（智能体驱动，2024-2025）

不再执行固定检索流程，而是让 **Agent 根据目标自主判断**下一步该查什么。详见 [[../topics/agentic-rag-patterns|Agentic RAG 四种模式]]。

- 四大模式：路由型（Routing）、多步型（Multi-step）、纠错型（CRAG）、自适应型（Adaptive）
- A-RAG（中科大）：层级检索接口（关键词/语义/文档块三级），自主策略选择
- NaviRAG（清华）：主动导航式长文阅读，F1 涨 4.8%

### 第五阶段：长上下文时代的 RAG（2025-）

随着 Gemini 2M、Claude 200K 等长上下文模型的普及，有人开始质疑"RAG 是否还需要"。答案是需要——但角色变了：

- RAG 不再是为了"把信息塞进有限的上下文窗口"
- 而是为了**精准筛选**——长上下文也难消化 100 本书的全部内容
- RAG 从"检索是因为窗口小"转变为"检索是因为注意力有限"
- **RAG 定位：从窗⼝补丁 → 注意力筛选器**

详见 [[第 8 期：企业为什么需要 RAG，而不是直接微调模型]]。

---

## Classic RAG：先把相似内容找出来

### 核心流程

```
文档 → 切块（Chunk）→ Embedding → 向量数据库 → 用户查询时：
  用户问题 → Embedding → Top K 检索 → 拼入上下文 → LLM 生成答案
```

关键点：**不是只给问题做 embedding**，文档在入库时已提前 embedding，查询时实时 embedding。

### 适用场景

- FAQ 问答、政策查询、产品手册问答
- 客服知识库、内部文档检索
- 任何「答案就在某个段落里」的问题

### 优势
- 简单、快、成本低、工具链成熟
- 行为可预测，结果可解释

### 局限
- 擅长「找到相关内容」，不擅长「沿着关系继续往下找」
- 遇到跨文档、跨实体的多跳问题容易卡住
- 例如「A 的经理的经理是谁？」——答案不在一个段落，而在关系链中

> Classic RAG 解决的是「**资料在哪里**」的问题。

---

## Graph RAG：不只找文本，还要找关系

### 核心机制

在文本检索之外增加一层**知识图谱**结构。实体 = 节点（人/产品/零件/团队/供应商/系统），关系 = 边（汇报/使用/来自/负责/购买）。

```
文本线：切块 → Embedding → 向量库（保留）
图谱线：抽取实体 + 关系 → 构建知识图谱 → 沿图谱路径遍历查询
```

### 典型问题

用户问「哪些产品会受到半导体短缺影响？」：
- Classic RAG 可能找到「半导体短缺」相关文本片段
- Graph RAG 沿路径走下去：供应商 S → 芯片 Z → 电路板 Y → 产品 X，精准找出受影响范围

### 适用场景

- 影响分析（一个变化影响哪些下游对象？）
- 依赖分析（某组件被哪些对象依赖？）
- 组织关系查询、审批链查询
- 供应链分析、因果链解释

### 代价

- **建图成本高**：从文本中准确抽取实体和关系需要模型能力
- **维护成本高**：业务世界持续变化，图谱需持续更新
- **边界限制**：只能遍历已建模的节点和边，未建模的领域无路径可走

> Graph RAG 不是用来替代 Classic RAG 的，而是用来增强那些「关系密集型」问题的。

> Graph RAG 解决的是「**信息之间如何连接**」的问题。

---

## Agentic RAG：让系统决定下一步该查什么

### 核心机制

不再执行固定检索流程，而是让 **Agent 根据目标自主判断**：下一步该查什么、用什么工具、证据够不够、要不要重新规划。

```
问题 → Agent 拆解 → 选择工具/数据源 → 执行查询 → 评估结果 → 
  证据不足？→ 换工具/换数据源 → 继续查询 → 综合判断 → 输出结论
```

### 典型问题

用户问「为什么产品销量下降了？」——没有固定资料入口，可能涉及：
1. 查询销售数据库确认下降趋势
2. 查询价格历史看是否有调价
3. 查询营销活动记录看投放是否减少
4. 查询客服工单看是否有集中投诉
5. 查询库存系统看是否有缺货
6. 证据不足时继续选择新工具或数据源

### Agent 可调用的工具

内部文档搜索、向量数据库、知识图谱查询、SQL 数据库、Web API、工单系统、日志系统、BI 报表……

### 适用场景

- 复杂问题分析、业务异常归因
- 跨系统调查、多数据源综合判断
- 技术排障、竞品分析、投资研究、运营复盘

### 代价

- **延迟高**：多轮工具调用，不可预测
- **成本高**：每次规划/判断/重规划都消耗 token
- **调试难**：不止看检索结果，还要看 Agent 为什么选这个工具、为什么跳过那个数据源

> Agentic RAG 不是默认更高级，只有真正需要它的场景才上——如果只是 FAQ，用 Agentic RAG 就像为拧一颗螺丝搬来一台挖掘机。

> Agentic RAG 解决的是「**面对复杂问题，下一步该怎么行动**」的问题。

---

## RAG 五类方案全景对比

除了三种核心架构，2025-2026 年衍生出更多技术路线。以下是五类 RAG 方案的一图对比：

| 维度 | 向量 RAG | GraphRAG | LightRAG | PathRAG | Vectorless RAG |
|------|----------|----------|----------|---------|----------------|
| **核心机制** | 语义相似度匹配 | 知识图谱+社区分层 | 知识图谱+双层检索 | 知识图谱+路径检索 | LLM 直接推理文档结构 |
| **多跳推理** | 弱 | 强（全局问答最强） | 中强 | 强（路径类） | 强 |
| **增量更新** | 简单 | 需重建（全局计算） | 原生支持 | 支持 | 支持 |
| **构建成本** | 低 | 高 | 中 | 中 | 低（但查询成本高） |
| **查询成本** | 低 | 中 | 中 | 中 | 高 |
| **成熟度** | 最高 | 高 | 中 | 较低 | 低 |
| **适用规模** | 百万~亿级文档 | 万级实体关系 | 万级实体关系 | 万级实体关系 | 千级文档 |

### LightRAG：GraphRAG 的轻量替代

GraphRAG 太重了？LightRAG 做了三个关键简化：
- 不划分社区，用**双层检索**（实体层+关系层）代替社区分层
- **增量更新**：新文档入库时只更新局部图谱，不用重建
- **去重机制**：相同实体自动合并

代价：全局问答能力不如 GraphRAG。

### PathRAG：路径感知的图检索

不关注社区，关注**路径**。在知识图谱上找到从查询实体到答案实体的「路径」，用路径上的信息作为上下文。适合供应链追溯、因果推理。

### Vectorless RAG（2026 新范式）

**完全抛弃向量数据库**。不切块、不向量化，让 LLM 直接「阅读」文档结构来推理定位信息。代表框架：PageIndex。

核心观点：分块本身就是有损操作——层次结构、交叉引用、上下文关系、视觉排版全丢了。向量检索再怎么优化，都是在「残缺数据」上做匹配。

适合少量但非常复杂的文档（合同、论文、技术规范），不适合海量文档。

### 选型决策树

```
你有哪些文档？
│
├─ 大量非结构化文档（FAQ、产品手册、新闻）
│   → 向量 RAG，够用且成熟
│
├─ 文档之间有复杂关系（法律、医疗、供应链）
│   ├─ 需要全局性问答 → GraphRAG
│   ├─ 数据经常更新，追求性价比 → LightRAG
│   └─ 主要查实体间路径/因果链 → PathRAG
│
├─ 少量但非常复杂的文档（合同、论文、技术规范）
│   → 试试 Vectorless RAG
│
└─ 复杂关系 + 大量文档 + 需要兼顾
    → 混合架构：向量检索 + 图谱增强（生产推荐）
```

**实战建议**：先向量 RAG 跑起来验证需求 → 遇到解决不了的问题（多跳推理、全局问答）→ 叠加图谱层形成混合架构 → 每加一层都要有明确的收益衡量。

---

## 三种架构对比（Classic / Graph / Agentic）

| 维度 | Classic RAG | Graph RAG | Agentic RAG |
|------|------------|-----------|-------------|
| **核心动作** | 检索 | 连接 | 推理 |
| **解决什么问题** | 资料在哪里 | 资料之间怎么连 | 下一步该查什么 |
| **适合问题** | 一跳问答、文档查询、FAQ | 依赖分析、影响分析、供应链 | 多步骤调查、跨系统分析 |
| **主要优势** | 快、简单、成本低 | 能沿关系路径查找和解释 | 灵活，能动态选择工具 |
| **典型代价** | 不擅长关系链和多跳 | 建图与维护成本高 | 延迟高、成本高、调试难 |
| **查询路径** | 固定 | 沿图谱遍历 | 动态规划 |

---

## 如何选择：看问题形状，不看架构名字

### 第一问：问题能否一步命中答案？
如果答案可以直接从某个文档片段中找到 → **Classic RAG**（产品保修期、政策适用范围、功能配置方式）

### 第二问：答案是否依赖关系链？
如果答案需要跨实体、跨文档、跨关系查找 → 加入 **Graph RAG**（谁影响谁、谁依赖谁、谁和谁有关）

### 第三问：查询路径能否提前定义？
如果一开始就不知道该查哪个系统、哪类数据 → 考虑 **Agentic RAG**（销量下降归因、故障排查、综合研究）

### 工程实践原则

**先用简单方案解决 80% 的问题，再为真正复杂的问题增加复杂度。**

```
1. 先上 Classic RAG → 解决高频、明确、低成本的问题
2. 遇到关系链问题 → 引入 Graph RAG
3. 遇到开放式多步骤跨系统调查 → 交给 Agentic RAG
```

架构的价值不在于名字，而在于它能不能匹配你的问题。

---

## RAG 架构的另一个维度：微调策略

Classic/Graph/Agentic 是从**检索方式**划分架构。另一个重要维度是**模型与检索器的训练关系**——是否对语言模型或检索器进行微调。

| 架构类型 | 修改模型参数 | 修改检索器参数 | 代表方法 | 适用场景 |
|---------|:-----------:|:-------------:|---------|---------|
| **无微调 RAG** | 否 | 否 | In-Context RALM | 快速落地、黑盒 API、原型验证 |
| **检索器微调** | 否 | 是 | REPLUG | 有黑盒大模型，但想优化检索质量 |
| **仅语言模型微调** | 是 | 否 | RETRO | 强化模型利用外部知识的能力 |
| **检索器与模型协同微调** | 是 | 是 | Atlas | 追求极致效果，训练资源充足 |

### 黑盒增强架构（不改模型参数）

适用于无法访问或修改大模型内部参数的场景（如使用 API 服务）。

**无微调**是最简单的形式：知识库、Embedding 模型、向量数据库和大模型相互独立，只通过 Prompt 注入检索结果。大多数企业 RAG 系统初期都采用这种形式。

**检索器微调（REPLUG）**的核心思想：如果某个文档能让语言模型更容易生成正确答案，检索器就应该给它更高权重。利用模型对不同文档的困惑度变化作为监督信号来训练检索器。

### 白盒增强架构（可修改模型参数）

适用于有开源模型且具备训练能力的场景。

**仅语言模型微调（RETRO）**：检索器保持预训练参数不变，大模型根据检索器提供的上下文信息对自身参数微调，强化「带着资料回答」的能力。

**检索器与模型协同微调（Atlas）**：二者同时微调以实现最佳协同。效果潜力最高，但训练复杂度也最高。

> 四象限与三种架构是正交关系。一个无微调的 RAG 系统可以是 Classic、Graph 或 Agentic。

---

## 知识库构建管线

RAG 系统的知识库构建主要涉及两个环节：

```
数据采集 → 预处理（清洗+分块） → 知识库增强（查询生成+标题生成） → 向量化入库
```

1. **数据采集与预处理**：多源数据整合为统一文档对象（携带元数据），经清洗（去噪）和分块后入库
2. **知识库增强**：
   - **查询生成**：用大模型为文档生成伪查询（"如果有人要查这段内容，可能会怎么问"），从查询角度表达文档语义，提升检索命中率
   - **标题生成**：为无标题文档自动生成合适的标题

详见 [[../topics/rag-optimization-techniques|RAG 优化 20 法]] 中「数据入库」阶段。

---

## RAG 系统评估

RAG 评估需要将检索层和生成层分开度量。详见 [[rag-evaluation|RAG 评估体系]]。

核心要点：
- **检索层**：Recall@K（召回覆盖）、MRR（首个相关排位）、NDCG（排序质量）
- **生成层**：Faithfulness（反幻觉）、Answer Relevancy（答即所问）、Context Precision/Recall（上下文纯度/覆盖）
- **测试集**：黄金集 + 对抗样本 + 版本冻结
- **工业级**：离线评估 + 在线评估 + 持续监控三条管线并行

---

## 优化策略：何时、何处、几次增强

RAG 并不意味着每次生成都必须检索。三个关键决策影响系统成本、延迟和答案质量。

### 何时增强（When to Augment）

核心判断依据：**大模型是否已有足够的内部知识来回答这个问题**。

| 方法 | 原理 | 局限 |
|------|------|------|
| **外部观测法** | 通过 Prompt 询问模型是否具备相关知识，或检查置信度/困惑度 | 模型可能「不知道自己不知道」 |
| **内部观测法** | 检测模型内部神经元或隐藏状态判断是否存储相关知识 | 需要白盒访问模型参数 |

实践中的最佳策略：**规则兜底 → LLM 判断 → 检索质量评估 → 证据充分性验证** 的决策链。

### 何处增强（Where to Augment）

检索结果可以在模型的不同位置注入：

| 位置 | 方式 | 特点 |
|------|------|------|
| **输入端** | 将检索文本拼入 Prompt | 主流方式，黑盒可用 |
| **中间层** | 检索向量通过交叉注意力融入隐藏状态 | 效果更深，但需要白盒访问 |
| **输出端** | 用检索结果校准已生成的文本 | 后处理方法，独立于生成过程 |

### 几次增强（How Many Times）

| 策略 | 做法 | 适用场景 |
|------|------|---------|
| **分解式增强** | 将复杂问题拆为子问题，迭代检索 | 多跳推理、需要多步查证 |
| **渐进式增强** | 将问题不断细化，分别检索覆盖 | 需要全面覆盖的开放式问题 |

> 📖 具体的优化手段（语义分块、混合检索、重排等）详见 [[../topics/rag-optimization-techniques|RAG 优化 20 法]]。

---

## RAG、Agentic RAG、AI Memory 三者的关系

这三个概念常被混淆，但解决的是不同层次的问题：

| 概念 | 一句话 | 解决的问题 |
|------|--------|-----------|
| **RAG** | 先查资料再回答 | AI 怎么接入外部知识 |
| **Agentic RAG** | 让 AI 自己决定怎么查 | 复杂问题里，AI 怎么自主选择检索策略 |
| **AI Memory** | 把有用的上下文留下，之后继续用 | 哪些上下文应该保留，跨对话复用 |

**三者不是替代，而是组合**：
- RAG 让它会查资料
- Agentic RAG 让它更会查资料
- Memory 让它能带着过去的上下文继续工作

当三者组合时，AI 才更像一个长期协作的助手，而不只是每次都要重新介绍背景的聊天窗口。

Memory 的关键不是「记住更多」，而是「记得更准，也忘得更对」。什么都记 → Memory 变垃圾堆；记错了 → 长期影响后续回答。

## 多模态 RAG

### 问题的本质

传统 RAG 管线假设输入是**纯文本**。但现实中的文档往往是图文混排的：图表、扫描件、PPT、视频截图等。多模态 RAG 要解决的核心问题是：**如何在检索和生成阶段处理非文本信息**。

### 两条路线

| 路线 | 做法 | 优势 | 代价 |
|------|------|------|------|
| **LLM + 工具链** | 先将 PDF/图片用解析工具转成文本，再送 LLM | 成本低、速度快、可控性强、长文档友好 | 解析层信息损失（图文关系丢失、复杂表格错位、排版语义消失） |
| **多模态模型** | 直接用多模态模型理解原始信息（ViT+CLIP+投影层） | 保留图文关系、理解图表语义、端到端推理 | 图片 Token 贵（5-10 倍成本）、视觉幻觉、可控性差 |

### 混合架构（推荐）

当前最务实的生产方案：

1. **优先走工具链**：格式规范的文档用解析工具，低成本高可靠
2. **兜底走多模态**：扫描件、图表密集页、工具链失败的内容，触发多模态模型处理
3. **多模态 Embedding**：用 CLIP 类模型将图片也向量化，文字 query 可直接召回相关图表

随着多模态模型成本持续下降，工具链→多模态的边界会逐渐向工具链方向移动。

> 详细对比见 [[../raw/articles/知识库/RAG 多模态 与 LLM+工具链|RAG 多模态原文]]。

### MinerU-Popo：文档解析通用后处理

在工具链方案中，PDF 解析是最薄弱的环节——段落被截断、表格被切断、标题层级乱、图片不知道属于哪段。

**MinerU-Popo** 是一个通用的文档解析后处理模型，专门解决这些问题：

- **文本截断恢复**：判断跨页段落是否应连接
- **表格截断恢复**：识别跨页表格并合并
- **标题层级重构**：建立完整的文档树层级
- **图像-文本关联**：将图片/表格挂到对应章节

核心思路：**不重做 OCR，只做后处理**。输入任意 OCR（MinerU/Paddle/GLM-OCR）的输出，输出结构化的文档树 + 节点摘要，直接给 RAG 使用。

> 底层模型：Qwen3-VL-4B 轻量微调，30K 训练样本，推理速度以「文档/秒」为单位。

## 从 RAG 到 LLM Wiki：知识库技术演进

[[AI 知识库技术演进拆解：从 RAG 到 NotebookLM，再到 LLM Wiki]] 指出，AI 知识库的技术路径经历三个阶段：

1. **第一阶段：低配 RAG** — 资料切块 → 向量化 → 检索 → 回答
2. **第二阶段：NotebookLM 类产品化 RAG** — 文档理解 → 多索引 → Retrieval and Ranking → Context Engineering → Source Grounding
3. **第三阶段：LLM Wiki / 深度知识库** — 知识抽取 → 实体识别 → 主题页生成 → 关系链接 → 冲突检测 → 增量更新 → 持续演化

三个阶段是逐层叠加的过程：低配 RAG 是底座，NotebookLM 把底座产品化/自动化/可信化，LLM Wiki 再把知识结构沉淀为长期资产。详见 [[../topics/obsidian-llm-wiki-practice|Obsidian LLM Wiki 实践]]。

> 📖 导航：[[overview|全局概览]] · [[../topics/rag-optimization-techniques|RAG 优化 20 法]] · [[../topics/production-rag-architecture|生产级 RAG 架构设计]] · [[knowledge-graph|知识图谱]] · [[rag-evaluation|RAG 评估体系]] · [[hybrid-retrieval|混合检索]] · [[../topics/agentic-rag-patterns|Agentic RAG 四种模式]] · [[../../教程/学习AI/06-知识库与RAG/00-知识库技术学习指南|知识库技术教程]]
