---
title: Dify 私有化部署
type: tutorial
tags: [Dify, Docker, 私有化部署, 配置]
created: 2026-06-02
updated: 2026-06-02
sources:
  - Wiki/wiki/entities/dify.md
related:
  - 00-Dify平台实战指南.md
  - 01-认识Dify.md
  - 03-RAG管线搭建.md
---

# 第二章：私有化部署

> 📖 用 Docker 一键部署 Dify，配置模型供应商和向量数据库。

---

## 2.1 Docker 部署（推荐）

**前置要求**：Docker & Docker Compose、2 核 4G+ 内存、20G+ 磁盘

```bash
# 1. 克隆仓库
git clone https://github.com/langgenius/dify.git
cd dify/docker

# 2. 复制环境变量配置
cp .env.example .env

# 3. 编辑 .env（关键配置项）
# SECRET_KEY=  # 生成随机字符串：openssl rand -hex 32
# INIT_PASSWORD=  # 管理员初始密码
# SANDBOX_API_KEY=  # 代码沙箱密钥

# 4. 启动服务
docker compose up -d

# 5. 验证
# 浏览器访问 http://localhost:80
# 初始管理员邮箱：根据 .env 配置
# 初始密码：根据 .env 中 INIT_PASSWORD

# 6. 查看运行状态
docker compose ps
docker compose logs -f api  # 查看 API 服务日志
```

### Docker Compose 核心服务

| 服务 | 端口 | 用途 |
|------|------|------|
| `api` | 5001 | 后端 API 服务 |
| `web` | 3000 | 前端界面 |
| `nginx` | 80/443 | 反向代理 |
| `db` | 5432 | PostgreSQL 数据库 |
| `redis` | 6379 | 缓存与消息队列 |
| `weaviate` | 8080 | 默认向量数据库 |
| `sandbox` | 8194 | 代码执行沙箱 |

---

## 2.2 关键配置项

```bash
# .env 关键配置

# === 模型配置 ===
# OpenAI
OPENAI_API_KEY=sk-xxx

# Claude
ANTHROPIC_API_KEY=sk-ant-xxx

# 国内模型（推荐至少配一个）
TONGYI_API_KEY=sk-xxx        # 阿里通义千问
ZHIPUAI_API_KEY=xxx           # 智谱 GLM
DEEPSEEK_API_KEY=sk-xxx       # DeepSeek（性价比最高）

# === 向量数据库 ===
# 默认 Weaviate 已内置，无需额外配置
# 如需切换 Milvus：
VECTOR_STORE=milvus
MILVUS_HOST=localhost
MILVUS_PORT=19530

# === 企业功能 ===
# 邮件服务（用于邀请成员、密码重置）
MAIL_TYPE=smtp
MAIL_DEFAULT_SEND_FROM=admin@yourcompany.com
SMTP_SERVER=smtp.yourcompany.com
SMTP_PORT=587
SMTP_USERNAME=your-email
SMTP_PASSWORD=your-password
```

---

## 2.3 初始设置

登录 Dify 后，第一步操作清单：

1. **配置模型供应商**：设置 → 模型供应商 → 添加至少一个
2. **创建管理员账号**：设置 → 成员 → 添加团队成员
3. **配置默认向量数据库**：设置 → 知识库 → 选择默认向量存储
4. **开启功能模块**：根据需要开启工作流/Agent/MCP 等功能

---

## 2.4 常见部署问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 启动后访问 404 | nginx 未正常启动 | `docker compose restart nginx` |
| 知识库上传失败 | 向量数据库未连接 | `docker compose logs weaviate` |
| API 调用超时 | 模型供应商未配置 | 检查 .env 中的 API Key |
| 沙箱执行失败 | sandbox 服务未运行 | `docker compose up -d sandbox` |

---

## 2.5 本章实战练习

- [ ] 用 Docker 完成 Dify 私有化部署
- [ ] 配置至少 2 个模型供应商（如 DeepSeek + 通义千问）
- [ ] 完成初始设置清单的 4 步操作

---

> 📖 继续学习 [[03-RAG管线搭建|第三章：RAG 管线搭建]]——创建知识库，搭建代码问答机器人。
